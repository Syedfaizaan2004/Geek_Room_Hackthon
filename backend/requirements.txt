# ============================================================
# Financial Research Agent — Python Dependencies
# Phase 0: Foundation Infrastructure
# ============================================================

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# --- Database (async SQLite / PostgreSQL-ready) ---
sqlalchemy>=2.0.36
aiosqlite>=0.20.0           # Async SQLite driver
asyncpg>=0.29.0             # Future: async PostgreSQL driver

# --- Settings / Config ---
pydantic>=2.8.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.1

# --- Vector Store ---
qdrant-client>=1.10.0

# --- LangGraph / LangChain (let pip resolve compatible set) ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0

# --- Auth (prepared for Phase 1) ---
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# --- HTTP / Async utilities ---
httpx>=0.27.0

# --- Logging ---
python-json-logger>=2.0.7

# --- Market Data & Analytics (Phase 3) ---
yfinance>=0.2.40              # Historical price data
pandas>=2.2.0                  # DataFrame operations
numpy>=1.26.0                  # Statistical calculations
sentence-transformers>=3.0.0   # Semantic embeddings (Phase 12)
