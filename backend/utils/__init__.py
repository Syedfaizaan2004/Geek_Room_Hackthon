"""
utils/__init__.py — Shared utilities package root.

Phase 0: Empty placeholder — helpers added progressively.

Future contents (Phase 1+):
  - auth.py        — JWT encode/decode, FastAPI get_current_user dependency
  - cache.py       — TTL in-memory dict cache for market data
  - pagination.py  — Cursor / offset pagination helpers
  - validators.py  — Ticker symbol validation, date range checks
"""
